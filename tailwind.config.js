const defaultTheme = require('tailwindcss/defaultTheme')
module.exports = {
  theme: {
    container: {
      center: true,
      padding: '2rem',
    },
    extend: {
      colors: {
      },
      fontFamily: {
      },
    },
  },
  variants: {},
  plugins: [
  ],
}
